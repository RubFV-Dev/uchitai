package io.github.rubfv.uchitai;

//Para teclas que solo se presionan y ya
public class NotaSimple extends Nota{
    //Nada de variables nuevas
    // Todo CONSTRUCTOR (RUBÉN)

	NotaSimple(int keycode, float tiempo) {
		super(keycode, tiempo);
	}
}
